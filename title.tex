$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$if(doi)$
\usepackage{etoolbox}
\makeatletter
\providecommand{\doi}[1]{% add DOI to \maketitle
  \apptocmd{\@title}{\par {\large \url{#1} \par}}{}{}
}
\makeatother
\doi{$doi$}
$endif$

$for(by-author)$
  \author{$by-author.name.literal$}
  $if(by-author.affiliations)$
    $for(by-author.affiliations)$
      \affil{%
        $if(by-author.affiliations.name)$
          $by-author.affiliations.name$
        $endif$
      }
    $endfor$
  $endif$
$endfor$

\date{$date$}
